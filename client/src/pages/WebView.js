import React from 'react';
import {Text, View, Button, SafeAreaView} from 'react-native';
function WebView({navigation}) {
  return (
    <SafeAreaView>
      <Text>어플리케이션 사용 방법을 불러오는 페이지 입니다.</Text>
    </SafeAreaView>
  );
}

export default WebView;
